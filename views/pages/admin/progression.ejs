<h3 class="mb-4">Progression Timeline</h3>

<%
  var expansions = [
    { name: 'Classic', color: '#c59a4a', phases: [
      { id: 0,  num: 1, title: "Launch — Molten Core, Onyxia's Lair" },
      { id: 1,  num: 2, title: 'Dire Maul — Azuregos, Lord Kazzak' },
      { id: 2,  num: 3, title: 'Blackwing Lair — Darkmoon Faire, AB' },
      { id: 3,  num: 4, title: "Rise of the Blood God — Zul'Gurub, Dragons of Nightmare" },
      { id: 4,  num: 5, title: "The Gates of Ahn'Qiraj — AQ20, AQ40" },
      { id: 5,  num: 6, title: 'Shadow of the Necropolis — Naxxramas, Scourge Invasion' }
    ]},
    { name: 'The Burning Crusade', color: '#4a9c3a', phases: [
      { id: 6,  num: 1, title: "Through the Dark Portal — Karazhan, Gruul's, Magtheridon" },
      { id: 7,  num: 2, title: 'Lords of Outland — SSC, The Eye, Season 2' },
      { id: 8,  num: 3, title: 'The Battle for Mount Hyjal — Black Temple, Season 3' },
      { id: 9,  num: 4, title: "The Gods of Zul'Aman" },
      { id: 10, num: 5, title: "The Sunwell — Sunwell Plateau, Isle of Quel'Danas, Season 4" }
    ]},
    { name: 'Wrath of the Lich King', color: '#3a7ac5', phases: [
      { id: 11, num: 1, title: 'Launch — Naxxramas, EoE, OS, VoA, Season 5' },
      { id: 12, num: 2, title: 'Secrets of Ulduar — Season 6' },
      { id: 13, num: 3, title: 'Call of the Crusade — ToC, Onyxia Revamp, Season 7' },
      { id: 14, num: 4, title: 'Fall of the Lich King — ICC, Season 8' },
      { id: 15, num: 5, title: 'Assault on the Ruby Sanctum' }
    ]}
  ];
%>

<form method="POST" action="/admin/progression">
  <input type="hidden" name="_csrf" value="<%= csrfToken %>">

  <!-- Current Phase Selector -->
  <div class="card bg-dark border-secondary mb-4">
    <div class="card-header border-secondary">
      <i class="fas fa-flag me-1"></i> Current Live Phase
    </div>
    <div class="card-body">
      <div class="row align-items-center">
        <div class="col-auto">
          <select name="current_phase" class="form-select bg-dark text-light border-secondary" style="width: 320px;">
            <% expansions.forEach(function(exp) { %>
              <optgroup label="<%= exp.name %>">
                <% exp.phases.forEach(function(ph) { %>
                  <option value="<%= ph.id %>" <%= currentPhase === ph.id ? 'selected' : '' %>>
                    Phase <%= ph.num %> — <%= ph.title %>
                  </option>
                <% }); %>
              </optgroup>
            <% }); %>
          </select>
        </div>
        <div class="col-auto">
          <span class="badge bg-success" style="font-size: 13px;">Currently: Phase <%= currentPhase %></span>
        </div>
      </div>
    </div>
  </div>

  <!-- Phase Release Dates -->
  <% expansions.forEach(function(exp) { %>
    <div class="card bg-dark border-secondary mb-3">
      <div class="card-header border-secondary" style="background: <%= exp.color %>22;">
        <span style="color: <%= exp.color %>; font-weight: bold;"><%= exp.name %></span>
      </div>
      <div class="card-body p-0">
        <table class="table table-dark table-sm table-hover mb-0">
          <thead>
            <tr>
              <th style="width: 60px;">#</th>
              <th>Phase</th>
              <th style="width: 60px;">Status</th>
              <th style="width: 180px;">Release Date</th>
            </tr>
          </thead>
          <tbody>
            <% exp.phases.forEach(function(ph) {
              var isLive = ph.id === currentPhase;
              var isPast = ph.id < currentPhase;
            %>
              <tr class="<%= isLive ? 'table-success' : (isPast ? '' : 'text-muted') %>">
                <td><strong><%= ph.num %></strong></td>
                <td><%= ph.title %></td>
                <td>
                  <% if (isLive) { %>
                    <span class="badge bg-success">LIVE</span>
                  <% } else if (isPast) { %>
                    <span class="badge bg-secondary">Done</span>
                  <% } else { %>
                    <span class="badge bg-dark border border-secondary">TBA</span>
                  <% } %>
                </td>
                <td>
                  <input type="date" name="date_<%= ph.id %>" class="form-control form-control-sm bg-dark text-light border-secondary"
                    value="<%= phaseMap[ph.id] || '' %>">
                </td>
              </tr>
            <% }); %>
          </tbody>
        </table>
      </div>
    </div>
  <% }); %>

  <button type="submit" class="btn btn-gold btn-lg">
    <i class="fas fa-save me-1"></i> Save Timeline
  </button>
</form>
